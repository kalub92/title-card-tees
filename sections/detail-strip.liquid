<div class="detail-strip">
  <div class="container detail-strip-inner">
    {% for block in section.blocks %}
      <div>
        {{ block.settings.text }}
        {%- unless forloop.last %} <span class="dot">●</span>{% endunless -%}
      </div>
    {% endfor %}
  </div>
</div>

{% schema %}
{
  "name": "Detail strip",
  "tag": "div",
  "class": "detail-strip-section",
  "blocks": [
    {
      "type": "item",
      "name": "Detail",
      "settings": [
        { "type": "text", "id": "text", "label": "Text", "default": "GARMENT-DYED COTTON" }
      ]
    }
  ],
  "presets": [
    {
      "name": "Detail strip",
      "blocks": [
        { "type": "item", "settings": { "text": "GARMENT-DYED COTTON" } },
        { "type": "item", "settings": { "text": "RELAXED FIT" } },
        { "type": "item", "settings": { "text": "CAPPED AT 250" } },
        { "type": "item", "settings": { "text": "PRINTED ON DEMAND" } },
        { "type": "item", "settings": { "text": "FREE RETURNS" } }
      ]
    }
  ]
}
{% endschema %}
